---
/**
 * Asymmetrical Bento — 12-col grid, gap-based, each cell is its own card.
 * Children are <BentoCell>s with col-span / row-span values that determine
 * the layout. On mobile each cell stretches full width.
 */
---

<div class="bento-12">
  <slot />
</div>
